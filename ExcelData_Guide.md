# 엑셀시트 기초데이터 입력 가이드

## 1. 개요

본 가이드는 단가 및 소급금액 계산 시스템에서 사용하는 엑셀 데이터 입력 시 반드시 준수해야 할 주요 원칙과 주의사항을 안내합니다.  
정확하고 일관된 데이터 입력은 시스템의 원활한 운용과 오류 없는 데이터 생성을 위해 필수적입니다.

---

## 2. 데이터 입력 기본 원칙

- **정확성**: 모든 입력값은 실제 데이터와 일치해야 하며, 오타·누락·잘못된 형식이 없어야 합니다.
- **일관성**: 동일 문서 내 동일 항목은 반드시 동일한 기준과 규칙을 따라야 합니다.
- **유효성**: 입력값은 시스템에서 허용하는 범위와 형식에 맞아야 하며, 필수 항목은 반드시 입력해야 합니다.

---

## 3. 사용자 입력 구간 항목

엑셀 입력구간은 크게 세 가지 제공 구간으로 이루어져 있습니다.

1. **시스템 제공 구간**
   1. YYMM : 연월
   2. VENDCD : 거래처코드
   3. VENDNM : 거래처명
   4. APNO : 품번
   5. IOGBN : 입출고구분코드
   6. KINDCD : 차종
   7. APNONM : 품명(ERP KOR Name)
   8. USEGBN : 단가구분코드
   9. COST : 단가
   10. SYMD : 시작일
2. **사용자 입력 구간 (필수 입력)**
   - 노란색으로 서식이 적용되어 있으며, 해당구역은 데이터를 미입력시 Error가 발생됩니다.
   1. NEW_USEGBN : 단가구분코드
   2. NEW_COST : 단가
   3. NEW_GAP : 신규단가와 기존단가의 차이
   4. NEW_SYMD : 변경 시작일
   5. NEW_EYMD : 변경 종료일
3. **사용자 입력 구간 (조건 입력)**
   - 초록색으로 서식이 적용되어 있으며, 해당구역은 데이터를 미입력해도 Error를 발생하지 않습니다.  
   1. MEMO : 원가영업팀 변경사유
   2. START : 소급 시작일
   3. END : 소급 종료일

---

## 4. 주요 입력 항목별 유의사항

### 4.1 거래처코드(VENDCD)

- 모든 행의 VENDCD 값은 반드시 동일해야 하며, 여러개의 거래처 코드가 혼합된 경우 데이터가 처리되지 않습니다.

### 4.2 날짜 입력(NEW_SYMD, NEW_EYMD)

- 날짜는 반드시 ‘YYYY-MM-DD’ 형식으로 입력해야 합니다.
- 시작일(NEW_SYMD)은 종료일(NEW_EYMD)보다 작아야 하며, 반대의 경우 오류로 간주됩니다.
- 날짜가 비어 있거나 잘못된 형식이면 데이터가 정상적으로 처리되지 않습니다.
- 현재 사용중인 단가는 종료일(NEW_EYMD)이 없음으로, 종료일(NEW_EYMD)을 입력하지 않습니다. (3.5 참조), 종료일(NEW_EYMD)이 NULL(비어있음)으로 입력데이터로 간주합니다. (3.3 참조)

### 4.3 중복 데이터(APNO, NEW_SYMD, NEW_EYMD)

- APNO, NEW_SYMD, NEW_EYMD 조합이 동일한 행이 2개 이상 존재할 경우 중복 오류로 간주되어 데이터가 처리되지 않습니다.
- 동일한 조합의 데이터가 중복 입력되지 않도록 각별히 주의하세요.

### 4.4 GAP 값(NEW_COST, COST, NEW_GAP)

- NEW_GAP 값은 NEW_COST - COST의 산출 결과와 반드시 일치해야 합니다.
- 계산 결과와 불일치하거나, 숫자가 아닌 값이 입력되면 오류로 처리됩니다.

### 4.5 종료일 미입력(EYMD)

- 동일한 APNO에서 종료일(NEW_EYMD)이 비어 있는 행이 2개 이상 존재하면 오류가 발생합니다.
- 동일 APNO에 대해 종료일이 없는 행은 1개만 허용됩니다.

### 4.6 날짜 형식

- 날짜는 반드시 ‘YYYY-MM-DD’ 형식으로 입력해야 하며, 비어 있거나 형식이 다르면 오류가 발생합니다.

### 4.7 필수 입력 항목

- 노란색 셀로 표시된 항목은 반드시 입력해야 하며, 누락 시 오류로 간주됩니다.
- 현재 단가, 시작일(NEW_SYMD), 종료일(NEW_EYMD) 등 필수 항목이 빈 값이면 저장 시 오류가 발생합니다.
- 종료일(NEW_EYMD)이 NULL(빈값)으로 필수 입력데이터로 간주합니다. (3.2 참조)

### 4.8 단가구분(NEW_USEGBN) 드롭다운

- 단가구분코드(NEW_USEGBN)은 제공된 목록(드롭다운)에서만 선택해야 하며, 목록에 없는 값을 입력하면 오류가 발생합니다.

### 4.9 입력 데이터형 유효성 검증

- GAP, EYMD 등 숫자/날짜 항목에 문자열, 특수문자 등 잘못된 형식이 입력되면 오류가 발생할 수 있습니다.
- 조건부 서식(빨간색, 노란색 등)이 적용된 셀은 오류 또는 필수 입력 항목임을 의미하므로 반드시 확인 후 입력해야 합니다.

---

## 5. 입력 검증 방법

- 입력 데이터의 유효성은 프로그램에서 Validation 기능으로 검증됩니다.
- Validation 시 동일 경로에 "현재파일명_validated_timestamp.xlsx" 형태로 검증 파일이 생성됩니다.
- 사용자 안내 오류 메시지를 확인하고, 해당 항목을 반드시 수정한 후 다시 저장하세요.
- 오류가 없을때까지 Load한 사용자 입력파일을 반복 수정합니다.

---

## 6. 사용자별 입력 데이터 검증 안내

- 기본 입력 데이터의 유효성은 프로그램에서 Validation 기능으로 검증됩니다.
- 기초데이터 Download시에 제공된 Guide의 엑셀파일이 추가시트로 제공됩니다.
- 제공된 가이드외에 사용자별 입력가이드를 추가적으로 작성해서 기초데이터 문서에 Guide 시트로 작성할 수 있습니다.
- Guide 문서의 포맷을 지켜서 작성하셔야 문제없이 사용자별 가이드가 생성됩니다.
- 프로그램 업데이트시 사용자별 가이드가 초기화 됩니다, 필요시 별도로 저장하시고, 업데이트이후 사용자별 가이드를 다시 작성하시기 바랍니다.

---

**정확한 데이터 입력은 시스템의 신뢰성과 효율성을 높이는 첫걸음입니다.  
기초데이터 입력 가이드의 내용을 숙지하고, 올바른 데이터 입력하시어 업무의 정확성을 유지하시기 바랍니다.**